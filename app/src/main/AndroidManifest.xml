<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aquino_Bembo_Finals">
        <activity
            android:name=".admin.AdminLogout"
            android:exported="false" />
        <activity
            android:name=".admin.AllRecordsView"
            android:exported="false" />
        <activity
            android:name=".admin.AllApplicationsView"
            android:exported="false" />

        <activity
            android:name=".admin.PendingApplicationsView"
            android:exported="false" />
        <activity
            android:name=".admin.AdminHome"
            android:exported="false"
            android:label="Admin Dashboard" />
        <activity
            android:name=".initial.Regisration"
            android:exported="false" />
        <activity
            android:name=".initial.Login"
            android:exported="true"
            android:theme="@style/Theme.Aquino_Bembo_Finals"> <!-- Login as main activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Aquino_Bembo_Finals.NoActionBar" />
    </application>

</manifest>